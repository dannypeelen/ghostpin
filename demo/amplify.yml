version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd demo
        - npm ci
    build:
      commands:
        - npm run build || echo "no build step"
  artifacts:
    baseDirectory: demo
    files:
      - '**/*'
  cache:
    paths:
      - demo/node_modules/**/*
